# v0 sandbox internal files
__v0_runtime_loader.js
__v0_devtools.tsx
__v0_jsx-dev-runtime.ts
.snowflake/
.v0-trash/
.vercel/
next.user-config.*

# Environment variables
# Cover every .env variant, not just *.local — a file named plain `.env`
# holding the Supabase service-role key must never reach the repo.
.env
.env.*
!.env.example

# Common ignores
node_modules
.next/
.DS_Storetest-results/
playwright-report/
