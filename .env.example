# The origin of the Olive Institute course platform deployment (a separate
# repo, separate deployment). next.config.mjs proxies /institute/:path* to
# ${INSTITUTE_ORIGIN}/institute/:path* — there is no plausible default, so
# the build fails loudly if this is unset rather than proxying nowhere.
INSTITUTE_ORIGIN="https://olive-institute.example.com"
